spring:
   datasource:
     url: jdbc:mysql://114.67.178.177:3306/TestDB
     username: root
     password: fxj123456
      # 使用druid数据源
      # type: com.alibaba.druid.pool.DruidDataSource
     driver-class-name: com.mysql.jdbc.Driver
    ## 该配置节点为独立的节点，有很多同学容易将这个配置放在spring的节点下，导致配置无法被识别
#   mybatis:
#    mapper-locations: classpath*:/com/gupao/xing/jian/mapper/*Mapper.xml  #注意：一定要对应mapper映射xml文件的所在路径
     #type-aliases-package: com.gupao.xing.jian  # 注意：对应实体类的路径